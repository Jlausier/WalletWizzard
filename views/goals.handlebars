<section>
    <div id="main" class=" main-content flex-1 bg-gray-700 mx-10 mb-10 pb-24 md:pb-5 rounded-2xl bg-gray-700">
        {{!-- Title Container --}}
        <h1 class=" rounded-t-3xl pl-12 pt-12 text-5xl text-gray-100">Goals</h1>
        <div class=" pt-6 flex flex-col bg-gray-700">
            <div class="flex justify-end">
                <button class="mr-2 px-5 py-2 rounded-full border-2 border-gray-200 text-sm text-gray-100 font-bold drop-shadow-lg hover:bg-gray-100 hover:text-gray-700 transition ease-in-out">
                    <i class="fas fa-plus income-btn"> Add New</i>
                </button>
            </div>
            {{!-- card container --}}
            <div class="p-6 flex flex-col w-full">
                {{!-- Content Card --}}
                {{#each goalData}}
                    {{> goals-progress-bar this}}
                {{/each}}
            </div>
        {{!-- Content Container Closer --}}
        </div>
    </div>
</section>