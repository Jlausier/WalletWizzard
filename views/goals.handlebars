     <section>
        <div id="main" class=" main-content flex-1 bg-gray-700 mx-10 mb-10 p-12 rounded-2xl bg-gray-700">

            {{!-- Title Container --}}
                <h1 class=" rounded-t-3xl text-5xl text-gray-100">Goals</h1>

           {{!-- Content Container --}}
            <div class=" pt-6 flex flex-col bg-gray-700">
                <div class="flex justify-end">
                    <a class="mr-2 px-5 py-2 rounded-full border-2 border-gray-200 text-sm text-gray-100 font-bold drop-shadow-lg">
                        +  Add New
                    </a>
                </div>

                {{!-- card container --}}
                <div class=" flex flex-col w-full">

                        {{!-- Content Card --}}

                        {{#each goal}}
                        {{>goals-progress-bar goal=.}}
                        {{/each}}

                </div>

                {{!-- Content Container Closer --}}
            </div>

        </div>
    </section>