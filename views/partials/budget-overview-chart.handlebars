<div id="budget-chart-container" class="w-full relative">
     <canvas id="myDoughnutChart" width="100" height="100"></canvas>

    <script>
      // Get the canvas element
      const ctx = document.getElementById("myDoughnutChart").getContext("2d");

      const expenseData = {{{json this}}};

      // Data for the doughnut chart
      const data = {
        labels: ["Essential Expenses", "Non-Essential Expenses", "Savings"],
        datasets: [
          {
            data: expenseData.sums.map(e => e.amount),
            backgroundColor: ["#d53369", "#daae51", "#D97560"],
          },
        ],
      };

      // Create the doughnut chart
      new Chart(ctx, {
        type: "doughnut",
        data: data,
        options: {
          cutout: "50%", // Adjust the hole size
        },
      });
    </script>
</div>